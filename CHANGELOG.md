# 更新日志

## v3.0（2026-09-23）

**四个新模块补齐 408 大纲 P0 缺口（42 → 46 个动画）；动画顺序改为与教材小节一致；逐帧读文案抓出 11 处"说的和做的不一致"。**

### 新增
- **并查集**（第5章）：双亲数组一个格子表示整片森林，三种策略对照同一串合并请求——不启用优化长出长链（find 退化 O(n)）、按大小合并压成星形、路径压缩把树高峰值 7 压到 1。Kruskal 判环背后的那个结构
- **外部排序**（第8章）：阶段一初始归并段可切"顺序分组 / 置换-选择"看段长差异，阶段二 k 路平衡归并画出**层间连线**（下一层的段是哪几个并出来的），趟数恒等于 ⌈log_k m⌉
- **链栈与链队列**（第3章）：链栈不带头结点、进栈拆成"先 s->next = top 再 top = s"两帧；链队列带头结点，重点演**队里只剩一个结点**时必须 `rear = front`。勾上选项可看漏写这句的后果——rear 悬在已释放内存上，再入队的 X 接不回队头
- **B 树与 B+ 树**（第7章）：三场景共用一棵真算出来的树。B 树插入（分裂上移、根分裂时长高一层）、B 树查找（逐层高亮比第几个关键字、走哪条指针）、B+ 树（叶子分裂上移的是**右半首键的副本**，叶子串成链表）
- **顺序重排**：动画排列顺序改为**教材小节顺序**，不再是开发先后。第2章 2.4→2.5→2.7→2.8、第3章 3.3→3.4→3.5→3.6、第5章 5.5→5.6→5.7、第7章 7.2→7.3（B 树回到动态查找表，不再排在散列之后）→7.4。第1/4/6/8 章本就同序

### 修复
- **并查集 6 处文案与实际算法不一致**：路径压缩模式的合并帧套用按大小的句式，把 1 个元素的树说成"不比 2 个的小"、实际却把大树挂到小树下面；交换句取的是交换**之后**的值，念出来是"只有 2 个、比 1 个小 → 换"；占位符不匹配导致帧文案打成"为 vv1 找根"；结论说"最大树高 1"而同一句又说"树高 7 → 1"；一次压缩改 7 个格子只标红 1 个；合并决策帧的高亮渲染层根本没读，最关键的一步只有文字没有图
- **外部排序**：`'…' + genMode === 'replace'` 先加后比、条件恒假，首句整段消失且不管用哪种方法都显示"顺序分组"；封段原因印成 `≥ lastOut()` 空括号；标题与底部趟数随翻页从"共 0 趟"跳到"共 2 趟"；`w=2` 时长到 12 个归并段，固定行距把最后两行画出 viewBox
- **伪代码"当前执行行"整体错位**：引擎的 `f.line` 是 CODE 的**数组下标**不是行号，并查集全按行号写；顺带抓到 complexity 与 sortGallery 两处下标越界（高亮一直是空的）
- **代码框不跟随高亮行滚动**：`.codebox` 是 `overflow:auto` 但翻帧从不滚动，手机竖屏只装得下约 9 行，长代码模块的"当前执行行"其实停在框外 600~900px 处——等于这个功能白做。改成只滚代码框本身
- **深链参数撞名**：`#m=<模块id>` 的 `m` 会灌进同名的"阶数"输入框，`run()` 抛错、整页零帧。引擎加保留字白名单，测试加全库闸门防回归

### 质量
- 断言 337 → **397**；手机视口卡口 34 → **48**
- 新增闸门：`frame.line` 必须是 code 的合法下标（全模块全帧）、输入框不得占用深链保留字、B 树/B+ 树结构不变式、外部排序元素总数守恒与趟数公式、链队列漏写特判后 X 真的丢失
- 几何检查扩到**多场景输入**（原来只喂默认值，多阶段模块的另一半版式没人查），并显式登记 3 处存量欠账（双向链表、哈夫曼、汉诺塔），每次跑都打印，不静默豁免
- 剥掉 16 处模块名里手写的圈号——编号由构建按位置自动生成，手写圈号在重排后全部会变成错的
- 手机卡口只用 headless Chrome 并指定独立临时 `--user-data-dir`，不再回退 Edge；缺 Chrome 或缺构建产物时跳过而非判失败

## v2.10（2026-09-22）

**三处"画到画布外/画成看不见"的渲染缺陷。**

### 修复
- **顺序表基本操作合集：整排格子画出画布，最左一格在 x=−69**（左边被吞、右边也看不见）。根因是格宽按**已填元素数**算（`cw = (W−140)/n − 10`），而空槽固定画到第 12 个 → 整排需要 1118 单位，画布只有 980。改为按 12 个槽位算格宽与起点，12 格全部进画面
- **一元多项式相加：结点间的箭头只有 4 个单位长，`next` 标签压在结点右边框上**。箭头长度 `step−18−(step−22) = 4`，等于一个点；标签居中公式 `x+nw+(step−22−nw)/2` 化简后就是 `x+nw`，永远落在边框上。改为结点间隙 22→34 单位、箭头 28 单位、标签居中在间隙正中（最小步长 88→104 以保证结点内部不被挤坏）
- **页头提示指向一个当前模式下不存在的按钮**："放映中按 ⋯ 展开完整操作台"在普通模式下也显示，而 `⋯` 只在放映态存在，让人去找不存在的东西。改成随模式切换：普通模式只提 ← → / 空格 / ☰，放映态提示 ← → / C / 右下角 ⋯ / Esc，手机放映提示左右箭头 / 捏合 / 双击 / ✕
- 附带：汉诺塔递归栈框右超画布 10 单位、拓扑排序输出行下超 5 单位，都是被新断言抓出来的

### 质量
- 断言 336 → **337**：新增「**无矩形画出 viewBox**」——原来只查文字越界，seqOps 的格子画到 x=1331 一直没被发现。全库扫出 4 处（seqOps / polyAdd 之外的 hanoi / topo），全部修掉
- **负向对照**：把 seqOps 格宽改回按已填数计算，断言立刻报出 `rect[-69,200]` 和 `rect[965,200]` 两侧越界——也证实了用户"左边被吞"的描述是准确的，我一开始按默认输入量测时只看到右侧溢出，差点误判成截图裁切

## v2.9（2026-09-22）

**总目录换成左侧滑出的深色抽屉。**

- 之前是居中的白色浮层 + 灰字小卡片，打开像弹出一个后台表格。现在改成从左侧滑出的抽屉，配色沿用页头那套深蓝主色（`#0f2c5c` / `#0c2449` / 悬停 `#17406f`），带宽度过渡动画，遮罩减淡；桌面宽 440px，手机上占满全宽
- **排版**：每条动画改成两行——名称 13.5px 加粗在上、教材小节 11.5px 灰字在下，行高 46→58px；章头加动画计数药丸、展开时整条高亮；搜索框 / 全部展开 / 关闭 收成一组等高的 34px（手机 44px）工具条；当前正在看的动画在目录里以左侧蓝条标出
- **单文件体积不变**：仍是 331KB。缩略图预览评估过——42 张代表帧 SVG 剥掉文字后仍要 +90~120KB，而 96×54 的预览里文字本来就读不出，性价比不成立，这次不做
- 断言 335 → **336 项**：新增「抽屉不超出视口宽度」；同时把「目录默认不超 1.5 屏」的量测对象从 `#catalog` 改成 `.ovlist`——抽屉自身高度固定，量错对象会让这条断言恒真。实测手机默认 **1.18 屏**、全展开 3.46 屏，桌面默认 1.0 屏

## v2.8（2026-09-21）

**修一个藏了很久的渲染缺陷：排序模块的图例根本画在 SVG 外面。**

### 修复
- **6 个排序模块（直接插入 / 希尔 / 冒泡 / 快排 / 直接选择 / 归并）的图例与补充文字被拼在 `</svg>` 之后**：`su.bars()` 内部已经用 `h.svg()` 包好返回，各模块又在其后 `g += su.legend(...)`，于是这些图元落在 SVG 之外、脱离 SVG 命名空间——浏览器把它们当未知 HTML 元素处理：**色块完全不渲染、四个标签挤成一行没有间距、坐标不按 viewBox 缩放、位置固定在画布底边之外被舞台裁掉**。这就是"底部那行要往下滑才能看到、初次进入看不到"的真正原因，不是版式或窗口高度问题。改为 `bars()` 只返回内部图元、各模块末尾统一 `su.wrap()`
- **按 Ctrl+C 复制会弹出总目录**：`C` 快捷键没排除修饰键。现在要求不带 Ctrl/Meta/Alt，且只在放映态生效（普通页面右上角就有 ☰ 目录按钮），页头提示同步改成"放映中按 C 打开目录"
- 手机放映的帧号曾有文本却 0×0 不显示：`#mpCounter{display:none}` 的默认规则没被 `body.mp` 覆盖，补 `display:block`

### 新增
- **手机放映底部解说条（仅手机端）**：放映时只看画面会不知道每一步在干什么，但再划一块高度给解说画布就又小了——所以让它浮在画面下缘、不占布局高度，右侧并上帧号

### 质量
- 断言 331 → **335**：新增「render 必须只返回一个单一完整 `<svg>`、`</svg>` 之后不得有游离图元」——覆盖全部 42 个模块每一帧。上面那条图例 bug 之前三条几何断言全都查不到，因为它们量的是字符串里的坐标，而浏览器根本没画这些元素。手机卡口 30 → 33 项，新增「箭头/解说条/帧号/退出 都已实际布局」，按矩形尺寸判而不按存在性判
- **负向对照**：把任一模块的 `su.wrap(g)` 改回 `g`，结构断言立刻红（insertSort 36 帧全中）；把放映缩放改回等比，字号断言红

## v2.7（2026-09-21）

**总目录改版：手机上从滑 6.6 屏变成 1 屏出头。**

- 目录原来是给桌面设计的卡片网格：42 张卡每张 112px、固定挂「打开/二维码/复制链接」三个按钮（合计 126 个按钮），实测手机竖屏要滑 **6.6 屏**、桌面 2.2 屏
- 改成**搜索 + 一行一条 + 按章手风琴**：顶部搜索框按名称/教材小节/章号过滤；默认只展开你正在看的这一章；「全部展开」一次摊开 42 条；每行的操作按钮改成悬停出现的 ▦（扫码直达）与 ⧉（复制链接），触屏上因无悬停而常显。实测手机默认 **1.2 屏**、全展开 3.0 屏，桌面默认 1 屏
- 修一个只有截图才能发现的 bug：`.ovrow{display:flex}` 把 `[hidden]` 的 UA 默认样式压掉了，搜索时 `hidden` 属性设了但行藏不起来，导致搜"哈夫"返回整章 4 条。补 `#catalog .ovrow[hidden]{display:none}`
- 手机卡口 28 → **30 项**：新增「目录默认不超 1.5 屏」「搜索后渲染可见行数真的只剩 1」——第二条刻意按 `offsetHeight` 数而不是读 `hidden` 属性，正是为了盯住上面这类"属性对了、渲染没变"的坑；负向对照把该规则改回 `display:flex` 后此条立刻红（剩 4 行）
- 全量：主套 301 项 + 手机/目录视口实测 30 项

## v2.6（2026-09-21）

**手机放映改成"看图软件"的样子：左右缘箭头翻页，画面严格铺满窗口。**

- **翻页按钮换成画面左右缘的 ‹ › 箭头**，底部那条"◀ 上一步 / 下一步 ▶"大按钮条整个去掉。底栏每占 1px 画布就少 1px，而手机横屏本来就是高度受限——去掉后舞台从 269px 涨到 **338px**，箭头浮在画布两侧的留白里，不压内容
- **放映下画面严格铺满窗口**，不再"按宽度铺满 + 上下拖动"。这是明确取舍：横屏 844px 下较高的画布（980×620~640）等比只能缩到 0.5 上下，标注约 5~6px（插入排序 7.3、堆排序 5.5、循环队列 5.4），要看清小字用双击放大（2.5×）或双指捏合。v2.4/v2.5 那套"铺满宽度换 8~9px"牺牲了完整性，按你的要求换回来
- 帧号改成底部居中的小圆牌；到头反馈（箭头变淡 + 提示 + 轻震）保留
- 手机卡口 25 → **28 项**，新增「画布完整落在窗口内（不需拖动）」——这条直接编码"不许拖"的要求；原「最小字号 ≥8px」放宽到 ≥5px 以匹配新设计，并留作防止再度缩小的下限
- 复核：桌面 1440×900 与 2556×1176 实测正常模式画布均未溢出舞台、控制条在视口内、body 不滚动——v2.5 那个"底部文字看不见"确实只由放映的铺宽规则引起，与页头/导航两行 chrome 无关

## v2.5（2026-09-21）

**修 v2.4 手机放映的一个过头决定：无条件按宽度铺满，把画面底部文字推出了屏幕。**

- 放映下改为**只在"宽度收益 ≥ 1.5 倍"时**才铺满宽度。手机横屏（舞台 844×269、画布 980×620）收益近 2 倍，铺满后字号 8~9px，用上下滚动换是值的；而桌面 `&mp=1` 下收益只有 1.3 倍，却把整段底部文字推到折叠区外——那种情况回到等比适配，整幅画面完整可见
- 画面确实溢出时，底部常驻一枚提示「↕ 画面比屏幕高，上下拖动可看其余部分」，取代原来一闪而过的一次性 toast
- 手机放映中不再弹"上课投影请用 ⛶ 放映"那条宽屏提示（对 mp 深链是噪音）

## v2.4（2026-09-21）

**手机放映：学生在手机上也能看清动画了。**

### 新增
- **⛶ 手机放映**：手机上点放映按钮进入独立形态——只剩画面加底部两个大按钮，伪代码、状态面板、导航全部收起；翻到首末步时按钮变灰，再点提示「已是第一步 / 已到最后一步」并轻震；右上角 ✕ 退出；支持 `&mp=1` 深链，分享出去就是"打开即放映"
- **放映下画布按宽度铺满**：横屏舞台约 844×269，而画布是 980×500~640 的横宽比，等比缩放属"高度受限"，字号被压到 4.5~5px 且左右各空一大块。改为铺满宽度后同样的字有 8~9px（实测堆排序 9.0、循环队列 8.6、Dijkstra 8.4），超出屏幕的部分上下拖动看
- **竖屏自动转横屏**：iPhone Safari 既不支持 div 的 `requestFullscreen` 也不支持 `screen.orientation.lock()`，所以用 CSS 旋转容器（H5 通行做法）实现，不依赖任何 API；安卓能进全屏就进，进不去也不影响铺满

### 修复
- **手机上章节说明被压成竖排**（"操作受限的线性表及其应用"一字一行），并因此把整条模块导航撑出约 200px 空白带：窄屏只给 `.pill` 加了 `flex:none`，漏了同容器里的 `.modnote`
- `fitCanvas` 改用 `offsetWidth/offsetHeight` 量舞台：`getBoundingClientRect` 返回的是变换后的视觉外框，容器被旋转 90° 时宽高互换，会把画布算小一半

### 质量
- 手机视口卡口 16 → **25 项**，新增"手机放映下画布最小字号 ≥8px"——这条是放映形态存在的唯一理由，等比缩放时它直接红到 4.5px
- **负向对照**：把放映缩放改回等比，三个模块的字号断言同时红（4.5 / 4.6 / 5.0）；桌面端 1400×900 截图复核无回归

## v2.3（2026-09-21）

**第三期：把课件从"教室投影"带到"学生手机"。**

### 新增
- **手机档布局（≤700px）**：390×844 实测舞台从 140–186px 提到 388px、页头从 171px 压到 85px、可点元素全部达到 44px 触摸标准（此前 25–35 个可点元素无一达标，最小 13px）。做法是新增窄屏断点：隐藏桌面键盘提示、章节与模块导航改横向滑动单行、伪代码框压到 170px、舞台 `min-height:46vh`——不动任何模块的绘制代码
- **触摸手势**：画面上单指横滑翻页（仅未放大时生效，放大后横滑是平移画面，二者不冲突）、双指捏合缩放、双击在适配与 2.5× 间切换以看清标注；手机首次进入给一次手势提示
- **概念节拍**：自动播放在"第 N 趟/轮/遍"边界多停 1.2 秒，`PageDown`/`PageUp` 按节拍跳转，底栏显示「· 节拍」。识别靠解说文本，全库仅 7 个模块命中、每模块 4–14 个节拍，不改各模块 `run()`
- **放映模式入口引导**：放映实测 42/42 模块字号达标，是唯一适合投影的形态，但入口藏在按钮里；宽屏首次进入提示一次并记住，目录页头加常驻说明

### 修复
- **图的 DFS 遍历：全部结点访问完后，"当前结点"橙圈还停在最后访问的点上**——递归返回到祖先继续扫描邻接点时，画面出现"人在 v6、却在扫 v2 的行"，而右侧递归栈已从 6 层塌成 1 层，学生看到的就是"点都绿了怎么还在动"。改为扫描时橙圈跟着"正在扫谁的行"走；这几帧加「回溯收尾」标记并说明"是递归返回前的收尾，不产生新结点"（它们是真实步骤：邻接表还没扫完，删掉会丢掉这个考点）
- **循环队列（环形场景）"待入队序列"整排格子压在底部解说条上**：序列带占 y 570–596、解说条占 590–624，重叠 6px。上移到 y=548（该处到 y≈500 之间本就是空白，不必撑大画布）。这是全库唯一一处"盒子撞盒子"，扫出来了
- **时间复杂度纵轴顶格不标数**（第二期把刻度取整改 `floor` 的遗留）：n=16 时轴顶是 65536 而最近刻度只到 10³，曲线最高点没有可读的数。改为补标实际最大值，且只在顶格刻度距轴顶 ≥12px 时才补——n=10 时 `yMaxLog=3.01`，按数值差判会和 `10³` 叠字

### 质量与工程
- **源码纳入版本控制**：`src/` 与 `tests/` 此前不在任何 git 仓库中（发布克隆里只有构建产物），改坏无法回滚。现建立源码基线仓库并打 tag `v2.2-src-baseline`，本期每个改动点独立可 revert
- 断言 **293 → 301**：新增手机视口卡口——headless 浏览器实测 390×844 四条（无横向溢出、可点元素 ≥44px、舞台 ≥45% 视口、页头 ≤100px），因 headless 有最小窗宽限制，改用 390px 宽 iframe 承载真实窄视口；新增概念节拍命中断言与轴顶标签几何断言；新增**「盒子不得互撞」**——两个带边框矩形部分重叠（互不包含）即判撞版，补上原"文字×文字"检查覆盖不到的一类；新增 DFS「当前结点跟随扫描行」不变量与收尾帧标注断言
- **负向对照**：CSS 退回 v2.2 基线时手机卡口红 12 条、恢复后 16/16 绿；轴顶守卫退回"只比数值差"时 2 条断言同时红；序列带坐标退回 588 时盒子互撞断言立刻红；抽掉 `cur = u` 时 DFS 不变量红——卡口是真的能拦住退化的

## v2.2（2026-09-21）

**从"能看"到"看得清、讲得动"：新增放映与缩放，修掉投影反向缩小的设计缺陷。**

### 新增
- **⛶ 放映模式**：全屏投放，隐藏全部导航 chrome，只留画面、翻页、播放、缩放、进度条；放映中按 `C` 直接切动画，`⋯ 更多` 展开完整操作台，`Esc` 退出
- **舞台缩放/平移**：1×–6×，`Ctrl`+滚轮或 －/＋ 按钮，放大后可拖拽平移；切换动画自动回到适配
- **自动播放按解说长度定帧**：约 1.1~2.2 秒/帧（原先固定 0.9s，而解说中位 35 字默读需 7s）；底栏新增 `⏱` 读数显示本帧停留；速度档补 0.25×
- 键盘 `C` 打开总目录（任意模式可用）

### 修复
- **投影模式此前越投影画面越小**：它只放大导航条字号，而画布字号写在 SVG 坐标系里不受影响，多占的高度全从舞台扣走（1280×720 实测画面从 136px 塌到 79px）。改为放大内容、收起副标题/章节条/页脚并压窄右栏，把高度还给舞台
- **使用引导默认展开挤掉一半舞台**（136→272px）：改为默认收起，进入动画时 toast 提示「点 ? 查看」
- 控制条在窄视口换行导致"最大规模 n"标签被压成竖排、进度条挤出可视区：改为输入项独占一行
- 点「适配」后画布停在 94% 不刷新：适配计算改用 border box，避免依赖当前滚动状态
- **多项式相加默认输入错误**：默认 B(x) 首项写成系数 0，首屏演示结果与教材例题不符（算法与对拍一直是对的，只有"打开就看得到"的默认值错）
- 评审整改批次（同日）：treeConvert 五帧画面完全相同、traversal 末帧统计为 0、空树输入崩溃、KMP/AVL/BST/分块 高亮行错位、sortGallery 稳定性列自相矛盾、迷宫足迹假值判定、括号全角归一、DFS 完成帧重复、输入 XSS 清洗、hashchange/Esc/浮层防重、build.py 单页标题锚定、仓库 src/tests 与发布物同步、zip 下载直链

### 质量与口径
- 断言 **281 → 288**；新增 `defVals()` 助手，补"默认输入跑出来的结果 = 教材答案"类断言，堵住"只测手工喂进去的值"的盲区
- 动画数校正为 **42**（`heapBuild` 已并入堆排序），此前 README/CHANGELOG/站点 meta 均写 43；站点文案数字改为由实际模块数自动推出
- README 删除已下线的「🎯 练习模式」宣传；第 6~8 章目录去掉会随增删整体错位的圈号序号

### 已知限制
- 第 6 章（图）6 个动画仍不支持自定义图，只能切预设
- 普通模式下画面内文字平均仍只有 8.5px（放映模式已 ≥12px）——根因是画布宽高比与舞台不匹配，需逐模块重做，见《画布几何分析报告》

---

## v2.1（2026-09-19）

**动画 33 → 42，断言 184 → 281，全部 8 章补齐教学常用算法。**

### 新增动画（M4 补强，10 个）
- 第 1 章：时间复杂度可视化（六阶增长曲线，n 可拖动）
- 第 2 章：顺序表基本操作合集、链表基本操作合集（与顺序表随机存取对照）、合并有序表 LA+LB→LC（归并铺垫）、双向/循环链表（四步插链/两步跨删）、一元多项式相加（教材例题 9x⁸ 抵消）
- 第 3 章：数制转换（1348→(2504)₈ 栈逆序）、迷宫求解（DFS 探索 + 足迹栈回溯）
- 第 5 章：树/森林与二叉树转换（加线/抹线/旋转三步双视图）、堆的建立与调整（自下而上筛选）

### 新功能（工作流 H）
- **单动画分享页**：`a/<动画id>.html`（43 个，各约 50KB）——分享/扫码只打开那一个动画，不再加载整个课件；目录的「二维码」「复制链接」均指向单页，页内「← 全部动画」可返回主站
- 构建时 terser 压缩：单文件 389KB → 252KB（-35%），线上传输再经 gzip 约 80KB
- 总目录每个动画支持生成**二维码**（手机扫码直达），基于内联 MIT 库 qrcode-generator
- ~~讲义导出~~：实测后认为价值不足，已移除

### 体验修复（用户反馈）
- 树/森林与二叉树转换重写为**结点旋转滑动动画**：6 帧（原树→加线→抹线→旋转中→完整二叉树→验证），终点用教材画法呈现完整的二叉树（绿实线=左孩子、蓝虚线=右孩子），每帧画面互异
- 复杂度可视化改为**对数纵轴**（每格 ×10）：曲线不再上顶重叠，O(n²) 近似直线、O(2ⁿ) 一路更陡，倍数关系真实可见；"最大规模 n"参数（4~64）真正生效，帧数随设定动态变化
- 练习模式重做：解说栏与状态面板**同时遮住**（原来状态面板会泄露答案），遮罩给出具体预测指引（"谁会被比较？谁会被移动？结果是多少？"），想好后点解说栏对答案

### 质量
- 断言 184 → 281（新增排序排列不变量、随机数据有序性、教材第二例 WPL=261、建堆随机数据堆性质、越界/格式错误场景等）
- seqStack/circQueue 补齐首帧使用引导

## v2.0（2026-09-19）

**新增 17 个动画，总数 16 → 33，覆盖《数据结构（C语言版）》全部 8 章核心算法，对齐考研 408 考纲。**

### 新章节
- 第 4 章 串和数组：模式匹配 BF/KMP 对照（next 数组逐格计算）、对称矩阵压缩映射 + 稀疏矩阵三元组快速转置
- 第 7 章 查找：顺序/折半查找（判定树 + ASL）、分块查找、二叉排序树（插入 + 删除三情形）、AVL 四种旋转、哈希表线性探测 / 链地址（教材例题 ASL 对拍）
- 第 8 章 排序：直接插入（含折半插入）、希尔、冒泡（提前终止）、快速（Partition + 调用栈）、直接选择、堆排序（树/数组双视图）、2 路归并、基数（分配/收集）、八大排序总览对比

### 新功能
- **总目录**：按章分组全景卡片，一键打开 / 复制深链接
- **练习模式**：隐藏解说，先预测下一步再揭示（🎯 按钮）
- **投影模式**：课堂投屏大字号（🖥 按钮，自动记忆）
- 模块自动编号 ①~㉝；排序动画支持「随机 / 有序 / 逆序 / 几乎有序」输入预设
- GitHub Actions 自动跑 184 项正确性断言

### 改进与修复
- 界面彻底去课程专有信息，教材章节通用化（外校可直接采用）
- 表达式求值浮层不再遮挡优先级表；拓扑排序顶点编号对齐教材（C1~C6）；循环队列默认进入循环队列场景
- 全部内容（代码 + 文案 + 素材）以 CC BY-NC-SA 4.0 授权

## v1.6（2026-09-13）
- 16 个动画覆盖第 2/3/5/6 章；单屏布局；进度条拖动修复；版本徽标；首次上线 GitHub Pages
