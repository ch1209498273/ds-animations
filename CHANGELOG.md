# 更新日志

## v2.2（2026-09-21）

**从"能看"到"看得清、讲得动"：新增放映与缩放，修掉投影反向缩小的设计缺陷。**

### 新增
- **⛶ 放映模式**：全屏投放，隐藏全部导航 chrome，只留画面、翻页、播放、缩放、进度条；放映中按 `C` 直接切动画，`⋯ 更多` 展开完整操作台，`Esc` 退出
- **舞台缩放/平移**：1×–6×，`Ctrl`+滚轮或 －/＋ 按钮，放大后可拖拽平移；切换动画自动回到适配
- **自动播放按解说长度定帧**：约 1.1~2.2 秒/帧（原先固定 0.9s，而解说中位 35 字默读需 7s）；底栏新增 `⏱` 读数显示本帧停留；速度档补 0.25×
- 键盘 `C` 打开总目录（任意模式可用）

### 修复
- **投影模式此前越投影画面越小**：它只放大导航条字号，而画布字号写在 SVG 坐标系里不受影响，多占的高度全从舞台扣走（1280×720 实测画面从 136px 塌到 79px）。改为放大内容、收起副标题/章节条/页脚并压窄右栏，把高度还给舞台
- **使用引导默认展开挤掉一半舞台**（136→272px）：改为默认收起，进入动画时 toast 提示「点 ? 查看」
- 控制条在窄视口换行导致"最大规模 n"标签被压成竖排、进度条挤出可视区：改为输入项独占一行
- 点「适配」后画布停在 94% 不刷新：适配计算改用 border box，避免依赖当前滚动状态
- **多项式相加默认输入错误**：默认 B(x) 首项写成系数 0，首屏演示结果与教材例题不符（算法与对拍一直是对的，只有"打开就看得到"的默认值错）
- 评审整改批次（同日）：treeConvert 五帧画面完全相同、traversal 末帧统计为 0、空树输入崩溃、KMP/AVL/BST/分块 高亮行错位、sortGallery 稳定性列自相矛盾、迷宫足迹假值判定、括号全角归一、DFS 完成帧重复、输入 XSS 清洗、hashchange/Esc/浮层防重、build.py 单页标题锚定、仓库 src/tests 与发布物同步、zip 下载直链

### 质量与口径
- 断言 **281 → 288**；新增 `defVals()` 助手，补"默认输入跑出来的结果 = 教材答案"类断言，堵住"只测手工喂进去的值"的盲区
- 动画数校正为 **42**（`heapBuild` 已并入堆排序），此前 README/CHANGELOG/站点 meta 均写 43；站点文案数字改为由实际模块数自动推出
- README 删除已下线的「🎯 练习模式」宣传；第 6~8 章目录去掉会随增删整体错位的圈号序号

### 已知限制
- 第 6 章（图）6 个动画仍不支持自定义图，只能切预设
- 普通模式下画面内文字平均仍只有 8.5px（放映模式已 ≥12px）——根因是画布宽高比与舞台不匹配，需逐模块重做，见《画布几何分析报告》

---

## v2.1（2026-09-19）

**动画 33 → 42，断言 184 → 281，全部 8 章补齐教学常用算法。**

### 新增动画（M4 补强，10 个）
- 第 1 章：时间复杂度可视化（六阶增长曲线，n 可拖动）
- 第 2 章：顺序表基本操作合集、链表基本操作合集（与顺序表随机存取对照）、合并有序表 LA+LB→LC（归并铺垫）、双向/循环链表（四步插链/两步跨删）、一元多项式相加（教材例题 9x⁸ 抵消）
- 第 3 章：数制转换（1348→(2504)₈ 栈逆序）、迷宫求解（DFS 探索 + 足迹栈回溯）
- 第 5 章：树/森林与二叉树转换（加线/抹线/旋转三步双视图）、堆的建立与调整（自下而上筛选）

### 新功能（工作流 H）
- **单动画分享页**：`a/<动画id>.html`（43 个，各约 50KB）——分享/扫码只打开那一个动画，不再加载整个课件；目录的「二维码」「复制链接」均指向单页，页内「← 全部动画」可返回主站
- 构建时 terser 压缩：单文件 389KB → 252KB（-35%），线上传输再经 gzip 约 80KB
- 总目录每个动画支持生成**二维码**（手机扫码直达），基于内联 MIT 库 qrcode-generator
- ~~讲义导出~~：实测后认为价值不足，已移除

### 体验修复（用户反馈）
- 树/森林与二叉树转换重写为**结点旋转滑动动画**：6 帧（原树→加线→抹线→旋转中→完整二叉树→验证），终点用教材画法呈现完整的二叉树（绿实线=左孩子、蓝虚线=右孩子），每帧画面互异
- 复杂度可视化改为**对数纵轴**（每格 ×10）：曲线不再上顶重叠，O(n²) 近似直线、O(2ⁿ) 一路更陡，倍数关系真实可见；"最大规模 n"参数（4~64）真正生效，帧数随设定动态变化
- 练习模式重做：解说栏与状态面板**同时遮住**（原来状态面板会泄露答案），遮罩给出具体预测指引（"谁会被比较？谁会被移动？结果是多少？"），想好后点解说栏对答案

### 质量
- 断言 184 → 281（新增排序排列不变量、随机数据有序性、教材第二例 WPL=261、建堆随机数据堆性质、越界/格式错误场景等）
- seqStack/circQueue 补齐首帧使用引导

## v2.0（2026-09-19）

**新增 17 个动画，总数 16 → 33，覆盖《数据结构（C语言版）》全部 8 章核心算法，对齐考研 408 考纲。**

### 新章节
- 第 4 章 串和数组：模式匹配 BF/KMP 对照（next 数组逐格计算）、对称矩阵压缩映射 + 稀疏矩阵三元组快速转置
- 第 7 章 查找：顺序/折半查找（判定树 + ASL）、分块查找、二叉排序树（插入 + 删除三情形）、AVL 四种旋转、哈希表线性探测 / 链地址（教材例题 ASL 对拍）
- 第 8 章 排序：直接插入（含折半插入）、希尔、冒泡（提前终止）、快速（Partition + 调用栈）、直接选择、堆排序（树/数组双视图）、2 路归并、基数（分配/收集）、八大排序总览对比

### 新功能
- **总目录**：按章分组全景卡片，一键打开 / 复制深链接
- **练习模式**：隐藏解说，先预测下一步再揭示（🎯 按钮）
- **投影模式**：课堂投屏大字号（🖥 按钮，自动记忆）
- 模块自动编号 ①~㉝；排序动画支持「随机 / 有序 / 逆序 / 几乎有序」输入预设
- GitHub Actions 自动跑 184 项正确性断言

### 改进与修复
- 界面彻底去课程专有信息，教材章节通用化（外校可直接采用）
- 表达式求值浮层不再遮挡优先级表；拓扑排序顶点编号对齐教材（C1~C6）；循环队列默认进入循环队列场景
- 全部内容（代码 + 文案 + 素材）以 CC BY-NC-SA 4.0 授权

## v1.6（2026-09-13）
- 16 个动画覆盖第 2/3/5/6 章；单屏布局；进度条拖动修复；版本徽标；首次上线 GitHub Pages
